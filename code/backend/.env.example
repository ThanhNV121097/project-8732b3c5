# Runtime database URL injected by compose/runtime.
DATABASE_URL=postgres://app:app_secret@localhost:5432/app?sslmode=disable

# HTTP port for API server.
PORT=8080

# Optional fallback HTTP port if PORT is unset.
APP_PORT=8080
