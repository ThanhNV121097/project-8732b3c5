# project-8732b3c5
A single web page that shows a list of saved notes. There is exactly ONE capability: display the notes already stored in the database, with a loading state, an empty state and an error state. No adding, no editing, no deleting, no search, no auth. One screen, one read-only list. Keep the plan to a single function.
